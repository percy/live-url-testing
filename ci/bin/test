#!/bin/bash
set -euo pipefail

echo "Test start!!"
export DEVICE_ID="abcd"
buildkite-agent meta-data set "host" "$BUILDKITE_BUILD_NUMBER__$BUILDKITE_JOB_ID" --job $BUILDKITE_JOB_ID
echo "***** $DEVICE_ID"
exit 3
